// We will make a function
function make_shirt (size: string, message: string){
     console.log("The size of the shirt is " + size + " and the message printed on it is: '" + message + "'.");
}

// Call the function with sample arguments
make_shirt('M', 'Coding is cool!');

Output:
      The size of the shirt is M and the message printed in it is: Coding is cool!
